#ifndef FUNCTIONS
#define FUNCTIONS

/**
* \brief compute a % b for floats
*/
// already defined in <cmath>
/*float fmod(float a, float b)
{
    while(a>b) a -= b;
    return a;
}*/

#endif // FUNCTIONS
